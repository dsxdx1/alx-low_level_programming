#include <stdio.h>
/**
 * main - gotta do better than last year
 * description - a-z
 * Return: returns zero
*/
int main(void)
{
	char alpha;

	for (alpha = 'a' ; alpha <= 'z' ; alpha++) {
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
